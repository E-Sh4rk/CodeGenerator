{
  "private": true,
  "scripts": {
    "format": "prettier --write \"html/**/*.{js,html,css}\"",
    "lint": "eslint html/",
    "prepare": "git rev-parse --is-inside-work-tree >/dev/null 2>&1 && mkdir -p .git/hooks && cp githooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit || true"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.8.1",
    "eslint-config-prettier": "10.1.8",
    "globals": "17.11.0",
    "prettier": "3.9.6"
  }
}
